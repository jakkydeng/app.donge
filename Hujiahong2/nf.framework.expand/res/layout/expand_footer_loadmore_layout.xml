<?xml version="1.0" encoding="utf-8"?>
<!-- ListView的底部 -->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="2dp"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    android:paddingTop="1dp" >

    <Button
        android:id="@+id/expand_footer_loadmore_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/listview_morebtn_selector"
        android:padding="10dp"
        android:textColor="#333333"
        android:text="更  多" />

    <ProgressBar
        android:id="@+id/expand_footer_loadmore_progressbar"
        style="?android:attr/progressBarStyleSmall"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:layout_margin="5dp"
        android:indeterminateDrawable="@drawable/expand_progressbar_bg"
        android:visibility="gone" />

</LinearLayout>